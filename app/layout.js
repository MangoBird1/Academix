import { Inter } from 'next/font/google';
import './globals.css';

const inter = Inter({ subsets: ['latin'], variable: '--font-inter' });

export const metadata = {
  title: 'Academix — Application Tracker',
  description:
    'A fluid teal-glass career and education application tracker built with Next.js, Firebase Firestore and Tailwind CSS.',
};

export default function RootLayout({ children }) {
  return (
    <html lang="en" className={inter.variable}>
      <body>{children}</body>
    </html>
  );
}
